import { autorun, observable } from '../src'
import { buffer, getPlainObj } from './utils'

test('basic box value', () => {
  // const b = buffer()
  // const s = new Set<any>(['a'])
  // const obSet = observable(s)
  // autorun(() => {
  //   b(obSet.size)
  // })
  // obSet.add('b')
  // expect(b.toArray()).toEqual([1])
})
